name: test
on: ["push"]
jobs: 
    run-shell-script:
         runs-on: ubuntu-latest
         steps:
           - name: run echo command
             run: echo  "Hello World"
           - name: run multilple command
             run: |
                node -v
                npm -v  
                uptime
                uname -a